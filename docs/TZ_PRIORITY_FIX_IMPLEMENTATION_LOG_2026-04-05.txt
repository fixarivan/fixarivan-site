================================================================================
FixariVan — журнал внедрения ТЗ PRIORITY FIX + v2-add
Дата старта: 05.04.2026
Назначение: фиксировать изменения по блокам для итоговой оценки
Связь: docs/TZ_PRIORITY_FIX_2026-04-05.txt, docs/TZ_PRIORITY_FIX_v2_add_2026-04-05.txt
================================================================================

Формат записей: [дата] Блок(и) ТЗ | файлы | кратко | статус

--------------------------------------------------------------------------------
Итерация 1 — 05.04.2026
--------------------------------------------------------------------------------

[05.04.2026] A, B (частично) | api/lib/order_supply.php
  Удалён fallback «одна карточка inventory_items по совпадению имени» в
  fixarivan_resolve_inventory_item_id_for_order_line. Разрешён источник id:
  существующий inventory_item_id, order_lines_json (inventory_item_id, SKU→id).
  Обновлён docblock.
  Статус: сделано.

[05.04.2026] A, B | api/inventory_movement.php
  Убраны: подстановка order_id из [REQ …] в notes карточки склада; подбор
  order_warehouse_line_id через fixarivan_find_order_warehouse_line_for_arrival
  по имени товара. Обновление строки заказа при приходе — только при явном
  order_warehouse_line_id в теле запроса (как из списка покупок / Track).
  Статус: сделано.

[05.04.2026] E (частично), ТЗ основной блок 4 | api/sqlite.php, save_order_fixed.php,
  api/lib/order_center.php
  Колонка orders.estimated_labor_cost REAL; при сохранении заказа заполняется
  числом (fixarivan_resolve_estimated_labor_cost: приоритет estimatedLaborCost,
  иначе разбор publicEstimatedCost / существующие поля).
  public_estimated_cost остаётся TEXT для подписи/совместимости.
  Статус: сделано.

[05.04.2026] E (частично) | api/lib/finance_lib.php
  В сводке по заказам за период суммируется estimated_labor по строкам заказов
  (число из БД, иначе fallback с public_estimated_cost для старых строк).
  Возврат: orders.estimated_labor_sum; в notes добавлено пояснение.
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 2 — 27.03.2026
--------------------------------------------------------------------------------

[27.03.2026] A, H (частично), F | api/lib/order_warehouse_sync.php, api/sqlite.php,
  save_order_fixed.php, api/order_track_order_lines.php
  Стабильный line_id в order_lines_json: fixarivan_ensure_order_line_ids (префикс OL-…).
  Колонка order_warehouse_lines.order_line_key; синхронизация из JSON при пересборке строк.
  Вызов ensure перед enrich SKU при сохранении заказа и в Track.
  Статус: сделано.

[27.03.2026] A, B | api/lib/order_supply.php, api/order_purchase_queue.php
  fixarivan_resolve_inventory_item_id_for_order_line(..., ?int $owl_id): при owl_id
  сопоставление JSON только по order_line_key ↔ line_id (дубликаты имён различимы).
  Без owl_id — прежний путь «первая строка с таким именем» (legacy).
  Статус: сделано.

[27.03.2026] dismiss по owl_id | api/sqlite.php, order_purchase_queue.php,
  order_purchase_queue_action.php
  purchase_list_dismissals.owl_id + UNIQUE (owl_id) WHERE owl_id IS NOT NULL.
  Запись dismiss с owl_id; фильтр очереди: сначала по owl_id, иначе legacy order_id+name_norm.
  Статус: сделано.

[27.03.2026] E (частично) | api/lib/finance_lib.php
  summary.orders_income_estimate, summary.orders_profit_estimate (продажа строк + труд
  минус закупка по заказам за период; не смешивается с кассой revenue).
  Статус: сделано.

[27.03.2026] H | tests/tz_order_line_duplicate_test.php
  Скрипт проверки: две строки с одним именем, разные line_id / owl → разные inventory_item_id.
  Статус: сделано.

[27.03.2026] G | fixarivan_find_order_warehouse_line_for_arrival — пометка @deprecated
  в docblock (вызов из inventory_movement уже убран в итерации 1).
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 3 — 27.03.2026
--------------------------------------------------------------------------------

[27.03.2026] P1 блок 2 (цепочка цен) | api/inventory_movement.php
  При приходе с order_warehouse_line_id: если unit_cost не передан в JSON — берётся
  purchase_price из order_warehouse_lines; движение пишется с этой себестоимостью.
  После прихода: при положительной закупке из строки — default_cost карточки, если был 0;
  при положительной продаже из строки — sale_price карточки, если был 0 (TZ: не терять цены).
  Статус: сделано.

[27.03.2026] Багфикс синхронизации склада заказа | api/lib/order_warehouse_sync.php
  INSERT в order_warehouse_lines включён столбец order_line_key (раньше был в execute, но не в SQL).
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 4 — 27.03.2026
--------------------------------------------------------------------------------

[27.03.2026] C (v2-add) | api/inventory_movement.php
  При order_warehouse_line_id обязателен контекст заказа: хотя бы одно из полей
  order_id, document_id, ref_id; значение должно совпадать с order_id строки owl
  (через fixarivan_order_id_variants_for_pdo). Иначе 403-логика: отказ с кодом order_context.
  Статус: сделано.

[27.03.2026] G (частично) | api/lib/order_warehouse_sync.php, save_order_fixed.php,
  order_track_order_lines.php
  fixarivan_prepare_order_lines_json_for_persist: ensure line_id + enrich SKU в одном вызове;
  мастер-форма и Track используют общую функцию вместо двух шагов.
  Статус: сделано.

[27.03.2026] C — UI прихода из очереди | js/inventory_sqlite_override.js
  В теле движения при pendingOwlArrival явно передаются order_id и document_id вместе
  с ref_id для прохождения серверной проверки.
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 5 — 27.03.2026
--------------------------------------------------------------------------------

[27.03.2026] A (v2-add), legacy off | api/lib/order_supply.php, tests/tz_order_line_duplicate_test.php
  fixarivan_resolve_inventory_item_id_for_order_line: без owl_id → 0; без order_line_key
  у строки owl → 0; сопоставление только line_id ↔ order_line_key + inventory_item_id / SKU в строке JSON.
  Удалён fallback по имени позиции. Тест: без owl ожидается 0.
  Примечание: строки без line_id нужно пересохранить (мастер/Track), чтобы заполнить order_line_key.
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 6 — 27.03.2026
--------------------------------------------------------------------------------

[27.03.2026] Backfill order_line_key | tools/backfill_order_line_keys.php
  CLI: для каждого order_id из order_warehouse_lines — ensure line_id в order_lines_json
  (запись в orders при изменении), затем заполнение order_line_key: 1) по индексу при
  совпадении числа строк owl и JSON; 2) иначе по совпадению inventory_item_id с строкой JSON.
  Режим --dry-run без записи. Транзакция на весь прогон (dry-run: rollback).
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 7 — 27.03.2026
--------------------------------------------------------------------------------

[27.03.2026] G | api/lib/order_track_lines.php, order_track_order_lines.php
  Нормализация строк Track, суммы по строкам, resolve SKU/balance вынесены в общий
  модуль; эндпоинт только подключает lib и оркестрирует транзакцию.
  Статус: сделано.

[27.03.2026] F / стабильные line_id в Track UI | track.html
  parseOrderLinesFromDoc / строка таблицы / collectOrderLinesFromTbody передают line_id
  (data-line-id), чтобы при сохранении не терялись ключи строк (склад заказа / очередь).
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 8 — 27.03.2026
--------------------------------------------------------------------------------

[27.03.2026] P2 блок 7 (напоминания / public_expected_date) | api/lib/order_supply.php,
  save_order_fixed.php, save_order_supply.php, order_track_order_lines.php, master_form.html
  fixarivan_supply_missing_expected_date_warning: есть закупка или строки не «со склада»,
  но public_expected_date пустая — текст предупреждения (календарь не создаёт напоминание).
  Добавление к supply_warning при успешном save_order_fixed и order_track_order_lines;
  save_order_supply: поле supply_date_warning при том же условии.
  master_form: alert после успешного создания показывает supply_warning и warehouse_warning.
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 9 — 27.03.2026
--------------------------------------------------------------------------------

[27.03.2026] P2 блок 8 | api/lib/order_client_portal.php, api/clients.php, client_portal.php,
  api/lib/viewer_i18n.php
  Общая группировка заказов и документов: fixarivan_group_orders_with_documents (ключ по
  order_id или document_id; сопоставление квитанций/счетов/отчётов по order_id и document_id).
  Портал: цепочка «клиент → заказы → id», документы только по выбранному заказу, затем блок
  позиций; убрана свалка всех документов внизу страницы.
  Статус: сделано.

[27.03.2026] P2 блок 9 | api/lib/document_templates.php
  В dt_css() зафиксированы общие правила для .dt-document--order и .dt-document--invoice
  (единый каркас с остальными PDF).
  Статус: сделано.

[27.03.2026] P2 блок 10 | api/lib/finance_lib.php, api/finance.php
  Экспорт kind=orders (orders_period): fixarivan_finance_orders_csv_flatten — колонки
  document_id, order_id, клиент, статусы, даты, estimated_labor_cost, parts_*, число строк
  и сумма продажи по строкам; UTF-8 BOM и разделитель как у остальных CSV.
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 10 — 27.03.2026 (P3)
--------------------------------------------------------------------------------

[27.03.2026] P3 расширение поиска | api/search_optimized.php, api/clients.php, track.html,
  clients.html
  Заказы в SQLite-поиске: order_id, internal_comment, подстрока в order_lines_json (SKU/текст).
  Клиенты: поиск также по order_id и document_id связанных заказов.
  Track: фильтр по email клиента, комментариям (публичный/внутренний), document_id/display_id
  вложенных документов; подсказка в поле поиска обновлена.
  Статус: сделано.

[27.03.2026] P3 аналитика / UX финансов | finance.html
  Карточка заказов: строка «Оценка работ (сумма)»; сводка: оценка выручки и прибыли по заказам
  (данные summary уже отдавал API). Подвал: пояснение orders_income_estimate из notes.
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 11 — 27.03.2026 (P3 / унификация портала)
--------------------------------------------------------------------------------

[27.03.2026] Портал: отчёты без order_id | api/lib/order_client_portal.php, client_portal.php,
  api/lib/viewer_i18n.php
  fixarivan_portal_orphan_mobile_reports: отчёты mobile_reports с тем же нормализованным телефоном,
  пустым order_id, не дублирующие уже подтянутые по заказам; отдельная карточка в портале.
  Связь: ORDER_UNIFICATION_ANALYSIS (отчёты без привязки к заказу не пропадают для клиента).
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 12 — 27.03.2026 (TZ v2-add блок C, без «последнего заказа»)
--------------------------------------------------------------------------------

[27.03.2026] fixarivan_resolve_client_and_order | api/lib/order_center.php, save_receipt_ultimate.php,
  save_invoice.php
  Резолв заказа: по-прежнему нет подстановки последнего заказа клиента. Добавлена безопасная
  ветка: второй аргумент — document_id акта (orderDocumentId из квитанции или счёта); заказ
  ищется в orders; если order_id в первом аргументе пустой и привязка только по document_id,
  то при ненулевом orders.client_id он должен совпадать с клиентом из формы (иначе order_id
  не подставляется). Явный order_id в запросе ведёт себя как раньше.
  Квитанция: normalizeReceiptRecord сохраняет order_document_id из payload.
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 13 — 27.03.2026 (invoice.html: orderDocumentId в payload)
--------------------------------------------------------------------------------

[27.03.2026] Счёт из Track / ссылок с order_id + document_id | invoice.html, api/lib/invoice_i18n.php
  Скрытое поле orderDocumentId; parseQuery заполняет его из document_id акта при связке
  order_id+document_id (или из order_document_id в URL); buildPayload передаёт orderDocumentId
  в save_invoice.php для fixarivan_resolve_client_and_order. Подсказка order_bind_hint при привязке.
  Статус: сделано.

--------------------------------------------------------------------------------
Итерация 14 — 27.03.2026 (P2 блоки 5–8, единая загрузка заказа, аудит)
--------------------------------------------------------------------------------

[27.03.2026] P2 блок 5 (календарь при пустой дате) | api/lib/order_supply.php
  fixarivan_create_supply_reminder: при пустой нормализованной public_expected_date —
  DELETE AUTO_SUPPLY для заказа перед выходом (согласовано с очисткой даты в Track).
  Статус: сделано.

[27.03.2026] P2 блок 6 (дубли JSON) | api/lib/order_json_storage.php, save_order_fixed.php,
  order_track_order_lines.php, save_order_supply.php, api/lib/order_supply.php,
  generate_beautiful_client_act.php
  Единая запись storage/orders и orders_tokens; общие каталоги.
  Статус: сделано.

[27.03.2026] P2 блок 7 (предупреждения даты) | api/order_track_patch.php, track.html, order_new.html
  supply_warning в ответе patch; alertTrackSupplyWarningIfAny в Track; order_new — текст после save.
  Статус: сделано.

[27.03.2026] P2 блок 8 (портал) | client_portal.php, api/lib/order_client_portal.php, viewer_i18n.php
  Порядок блоков документы → позиции; fixarivan_portal_public_order_lines; сумма через
  fixarivan_orders_estimate_from_lines_json; подсказки documents_order_hint (ru/en/fi).
  Статус: сделано.

[27.03.2026] Единая загрузка заказа | api/lib/order_document_load.php, get_document.php, client_portal.php
  fixarivan_merge_order_json_overlay и fixarivan_load_order_from_sqlite_or_json; портал
  подгружает featured через тот же путь, что get_document?type=order.
  Статус: сделано.

[27.03.2026] Исправление (аудит) | client_portal.php
  Вызов fixarivan_portal_public_order_lines вместо удалённой client_portal_public_lines.
  Статус: сделано.

[27.03.2026] Документация | docs/REPORT_2026-04-05.txt, docs/CHANGES_2026-04-05.txt,
  docs/AUDIT_SESSION_2026-03-27.txt
  Обновление отчёта на 05.04.2026, список изменений и полный аудит сессии.
  Статус: сделано.

--------------------------------------------------------------------------------
Ожидает следующих итераций
--------------------------------------------------------------------------------
  • При появлении новых точек редактирования строк — подключать order_track_lines.php.

--------------------------------------------------------------------------------
Сводка итерации 1 (оценка изменений)
--------------------------------------------------------------------------------
  Риск регрессии: приход на склад больше не привязывает строку заказа без явного
  order_warehouse_line_id — клиентский сценарий должен передавать id (Track /
  список покупок). Удалён обходной путь «одна карточка по имени» в supply.

  Новые/изменённые артефакты:
  • orders.estimated_labor_cost + расчёт при сохранении + order_center INSERT
  • finance overview: orders.estimated_labor_sum

--------------------------------------------------------------------------------
Сводка итерации 2 (оценка)
--------------------------------------------------------------------------------
  • Очередь закупок и сопоставление карточки склада к строке заказа опираются на
    связку owl_id ↔ order_line_key ↔ line_id; совпадение только по имени остаётся
    запасным путём без контекста owl.
  • Dismiss «из списка покупок» фиксируется по owl_id; старые записи по имени
    продолжают работать.

--------------------------------------------------------------------------------
Сводка итерации 3 (оценка)
--------------------------------------------------------------------------------
  • Приход из очереди закупок без ручного unit_cost получает закупочную цену из строки
    заказа; карточка склада подтягивает цены, если были нулевыми.
  • Исправлен критический INSERT при синхронизации строк с order_line_key.

--------------------------------------------------------------------------------
Сводка итерации 4 (оценка)
--------------------------------------------------------------------------------
  • Приход по строке заказа без явного контекста заказа на сервере отклоняется;
    UI передаёт order_id и document_id из очереди покупок.
  • Подготовка order_lines_json к сохранению централизована для мастер-формы и Track.

--------------------------------------------------------------------------------
Сводка итерации 5 (оценка)
--------------------------------------------------------------------------------
  • Очередь закупок всегда передаёт owl_id; привязка карточки к JSON только по стабильным ключам.
  • Старые заказы без line_id в JSON / без order_line_key потребуют пересохранения.

--------------------------------------------------------------------------------
Сводка итерации 6 (оценка)
--------------------------------------------------------------------------------
  • Старые БД можно привести к ключам без ручного открытия каждого заказа: сначала
    dry-run, затем запуск без флага.

--------------------------------------------------------------------------------
Сводка итерации 7 (оценка)
--------------------------------------------------------------------------------
  • Логика строк Track на сервере в одном файле; клиент не генерирует новые line_id
    при каждом сохранении из-за потери поля в форме.

--------------------------------------------------------------------------------
Сводка итерации 8 (оценка)
--------------------------------------------------------------------------------
  • Пользователь видит явное предупреждение, если закупка есть, а ожидаемая дата для клиента
    не задана — иначе напоминание в календаре по закупке не появится.

--------------------------------------------------------------------------------
Сводка итерации 9 (оценка)
--------------------------------------------------------------------------------
  • Клиентский портал следует иерархии заказ → документы → позиции; списки PDF/квитанций не
    смешивают несколько заказов.
  • Экспорт заказов в CSV из финансов даёт предсказуемые заголовки вместо десятков полей БД.

--------------------------------------------------------------------------------
Сводка итерации 10 (оценка)
--------------------------------------------------------------------------------
  • Поиск по номеру заказа и акту доступен из списка клиентов, Track и API search_optimized;
    в финансах на экране видны оценки по заказам рядом с кассовой сводкой.

--------------------------------------------------------------------------------
Сводка итерации 11 (оценка)
--------------------------------------------------------------------------------
  • Диагностические отчёты без order_id остаются доступны клиенту в портале по совпадению телефона.

--------------------------------------------------------------------------------
Сводка итерации 12 (оценка)
--------------------------------------------------------------------------------
  • Привязка квитанции/счёта к заказу по document_id акта согласована с клиентом в БД; явный
    order_id не ужесточали, чтобы не ломать старые сценарии.

--------------------------------------------------------------------------------
Сводка итерации 13 (оценка)
--------------------------------------------------------------------------------
  • Форма счёта передаёт document_id акта на сервер при открытии из Track; резолв заказа
    согласован с квитанцией.

--------------------------------------------------------------------------------
Сводка итерации 14 (оценка)
--------------------------------------------------------------------------------
  • Календарь закупок не оставляет AUTO_SUPPLY при снятой дате; запись JSON заказа на диск
    в одном месте; пользователь видит предупреждение о закупке без public_expected_date
    из Track patch и order_new; портал следует порядку ТЗ и согласован с get_document;
    после аудита исправлен критический вызов функции строк в портале.

--------------------------------------------------------------------------------
Конец журнала (обновлять по мере прохождения блоков)
================================================================================
